{
  "name": "@esx/shared",
  "version": "1.0.0",
  "author": "wowjeeez",
  "license": "MIT",
  "scripts": {
    "build": "yarn && echo \" This module shouldn't be built standalone, linking is done from the `@esx/server` and `@esx/client` packages.\"",
    "test": "jest"
  },
  "dependencies": {
    "@reincarnatedjesus/f-chain": "^1.1.7"
  },
  "devDependencies": {
    "@citizenfx/client": "^2.0.4793-1",
    "@citizenfx/server": "^2.0.4793-1",
    "jest": "^27.5.1",
    "ts-jest": "^27.1.3",
    "typescript": "^4.6.2"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/?(*.)+(spec|test).ts?(x)"
    ],
    "globals": {
      "ts-jest": {
        "tsConfigFile": "server/tsconfig.json"
      }
    }
  }
}