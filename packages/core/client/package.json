{
  "name": "@esx/client",
  "version": "1.0.0",
  "author": "wowjeeez",
  "license": "MIT",
  "scripts": {
    "build": "yarn && rimraf dist && tsc --outDir dist && dts-bundle --configJson dts-bundle.json",
    "test": "jest"
  },
  "dependencies": {
    "uuidv4": "^6.2.12"
  },
  "devDependencies": {
    "@citizenfx/client": "^2.0.4793-1",
    "@types/jest": "^27.4.1",
    "dts-bundle": "^0.7.3",
    "jest": "^27.5.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.1.3",
    "typescript": "^4.6.2"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/?(*.)+(spec|test).ts?(x)"
    ],
    "globals": {
      "ts-jest": {
        "tsConfigFile": "server/tsconfig.json"
      }
    }
  }
}
